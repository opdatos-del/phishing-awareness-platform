import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { AnalyticsInsights as Insights } from '../api'

export function AnalyticsInsights({ insights }: { insights: Insights }) {
  const timing = insights.timing
  return <section className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><article className="rounded-2xl border border-slate-200 bg-white p-6"><p className="text-xs font-bold uppercase tracking-[.15em] text-[#d95d39]">Eficacia</p><h2 className="mt-1 font-serif text-2xl">Plantillas por tasa de clic</h2>{insights.templates.length ? <div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={insights.templates.slice(0, 6)} layout="vertical" margin={{ left: 24 }}><XAxis type="number" unit="%" /><YAxis type="category" dataKey="name" width={130} tick={{ fontSize: 11 }} /><Tooltip formatter={value => `${value ?? 0}%`} /><Bar dataKey="clickRate" name="Tasa de clic" fill="#d95d39" radius={[0, 6, 6, 0]} /></BarChart></ResponsiveContainer></div> : <p className="mt-5 text-sm text-slate-500">Aún no hay envíos para comparar.</p>}</article><article className="rounded-2xl bg-[#102a43] p-6 text-white"><p className="text-xs font-bold uppercase tracking-[.15em] text-[#f2a27e]">Decisión</p><h2 className="mt-1 font-serif text-2xl">Próximos re-test</h2>{insights.retestCandidates.length ? <div className="mt-5 grid gap-3">{insights.retestCandidates.slice(0, 5).map(candidate => <div key={candidate.recipientId} className="border-b border-white/10 pb-3"><div className="flex justify-between gap-3"><b>{candidate.name}</b><span className="text-[#f2a27e]">{candidate.riskScore} pts</span></div><p className="text-xs text-slate-400">{candidate.email} · {candidate.submits} caídas sin reporte</p></div>)}</div> : <p className="mt-5 text-sm text-slate-400">No hay personas pendientes de re-test.</p>}<div className="mt-6 border-t border-white/10 pt-4 text-sm"><p>Promedio de apertura: <b>{timing.averageOpenMinutes} min</b></p><p className="mt-1">Promedio hasta clic: <b>{timing.averageClickMinutes} min</b></p></div></article></section>
}
